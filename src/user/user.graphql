type User {
  id: Int!
  email: String!
  password: String!
}

input CreateUserInput {
  email: String!
  password: String!
}

type Query {
  login(email: String!, password: String!): User
}

type Mutation {
  createUser(data: CreateUserInput): User
}
